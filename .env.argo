# © 2025 Casey Koons All rights reserved
#
# Argo Project Configuration
# This file is required and must be in the project root.
# Override settings per-developer in .env.argo.local (not committed)

# ============================================================================
# AI Provider Configuration
# ============================================================================

# Default provider for workflow execution
# Options:
#   claude_code    - Claude Code CLI (no API key, uses Max account) [DEFAULT]
#   claude_api     - Claude API (requires ANTHROPIC_API_KEY)
#   openai_api     - OpenAI API (requires OPENAI_API_KEY)
#   gemini_api     - Google Gemini API (requires GEMINI_API_KEY)
#   grok_api       - xAI Grok API (requires GROK_API_KEY)
#   deepseek_api   - DeepSeek API (requires DEEPSEEK_API_KEY)
#   openrouter     - OpenRouter multi-model API (requires OPENROUTER_API_KEY)
#   ollama         - Ollama local server (requires ollama on port 11434)
ARGO_DEFAULT_PROVIDER=claude_code

# Default model for the selected provider
# Claude Code models: claude-sonnet-4-5, claude-opus-4, claude-sonnet-3-7
# Claude API models: claude-sonnet-4-5-20250929, claude-opus-4-20250514
# OpenAI models: gpt-4, gpt-4-turbo, gpt-3.5-turbo
# Gemini models: gemini-2.0-flash-exp, gemini-1.5-pro
# Grok models: grok-beta, grok-2
# DeepSeek models: deepseek-chat, deepseek-coder
# Ollama models: codellama, llama2, mistral
ARGO_DEFAULT_MODEL=claude-sonnet-4-5

# ============================================================================
# API Keys
# ============================================================================
# DO NOT put API keys in this file - it's committed to git!
# Put your API keys in .env.argo.local instead (not committed)
# See .env.argo.local.example for template
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
# Test marker - can be removed
